services:
  - type: web
    name: uefa-greatest-debate
    runtime: node
    plan: free
    region: virginia
    autoDeploy: false
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /healthz
    envVars:
      - key: PORT
        value: 10000
